export const authReducer = (state = { firstName: '', loading: false, token: '' }, action) => {
    const { type, payload } = action

    switch (type) {
        default:
            return state
    }
}